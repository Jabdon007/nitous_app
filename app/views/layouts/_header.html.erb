  <nav class="navbar navbar-inverse">
    <div class="container-fluid">
        <!-- Brand and toggle get grouped for better mobile display -->
        <div class="navbar-header">
            <button type="button" class="navbar-toggle collapsed" data-toggle="collapse" data-target="#bs-example-navbar-collapse-9">
                <span class="sr-only">Toggle navigation</span>
                <span class="icon-bar"></span>
                <span class="icon-bar"></span>
                <span class="icon-bar"></span>
            </button>
            <a class="navbar-brand" href="home">Turnip!</a>
        </div>

        <!-- Collect the nav links, forms, and other content for toggling -->
      <%=  %>
        <div class="collapse navbar-collapse" id="bs-example-navbar-collapse-9">
            <ul class="nav navbar-nav">                
                <li class="<%= params[:action] == 'home' ? 'active' : '' %>"> <%= link_to "Home", {controller: 'static_pages', action: 'home'} %> </li>
           <!-- <li><%= link_to "Home",'home' %></li>              -->
                <li class="<%= params[:action] == 'about' ? 'active' : '' %>"><%= link_to "About",{controller: 'static_pages', action: 'about'} %></li>
            </ul>
           <ul class="nav navbar-nav navbar-right">
             <% if !logged_in %>
             <li class="<%= params[:action] == 'new' && params[:controller] == 'users' ? 'active' : '' %>"><%= link_to "Sign Up",{controller: 'users', action: 'new'} %></li>
             <li class="<%= params[:action] == 'new' && params[:controller] == 'session' ? 'active' : '' %>"><%= link_to "Sign in",{controller: 'session', action: 'new'} %></li>
             <% else %>
              <li class="dropdown">
                <a href="#" class="dropdown-toggle" data-toggle="dropdown" role="button" aria-expanded="false"><%= @current_user.email %> <span class="caret"></span></a>
                <ul class="dropdown-menu" role="menu">
                  <li><%= link_to "Profile",current_user %></li>
                  <li class="divider"></li>
                  <li><%= link_to "Log out",logout_path, method: "delete" %></li>
                </ul>
              </li>         
             <% end %>                
          </ul>
        </div>
    </div>
</nav>